apiVersion: v1
kind: Service
metadata:
  name: {{ include "ryumem.fullname" . }}
  labels:
    {{- include "ryumem.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ryumem.selectorLabels" . | nindent 4 }}
